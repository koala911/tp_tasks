name: CI

on:
  push:
    branches: [master, task4]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: cloning repo
      uses: actions/checkout@v1

    - name: check directory
      run:
        ls -a

    - name: Build
      run: |
        cd ./task4/C
        mkdir build
        cd build 
        cmake ..
        make